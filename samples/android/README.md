# Android Config Samples

These folders show the intended file structure for each Android preset.

Each preset folder contains:

- `Engine.ini`
- `DeviceProfiles.ini`
- `GameUserSettings.ini`

The files are compact structure samples, not full generated output. Use the Config Generator page to create complete configs for a real device.
