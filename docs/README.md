# Project Notes

This folder is for local project notes and archived session transcripts.

`session-logs/` is intentionally ignored by git because those files are conversation exports, not website runtime assets.
